# Repository Instructions

- Never use em dashes.
- Do not run `npm build` unless new packages are added.

## Production delivery

- Distinguish `pushed`, `merged`, and `deployed` accurately.
- Before declaring prod ready, merge the PR, confirm deployment success, and smoke-test the prod endpoint.
- Report the PR, production commit SHA, deployment status, and smoke-test result.
